Дано
+-----------------------------------------------------+
|   transactions                                      |
+-----------------------------------------------------+
|   id - идентификатор записи (PK)                    |
|   id_transaction - идентификатор транзакции         |
|   card_id - номер карты                             |
|   maincard_id - номер мастер-карты                  |
|   date - дата и время траназакции                   |
|   sum - сумма транзакции                            |
|   type - тип транзакции                             |
|   employee - сотрудник                              |
|   doc_id - идентификатор чека                       |
|   cash_id - номер кассы                             |
|   shop_id - номер магазина                          |
|   doc_type - тип документа                          |
|   disc_id - идентификатор скидки                    |
|   disc_source - источник скидки                     |
+-----------------------------------------------------+
Задание
Найти количество чеков в период дат 2019-10-25 - 2019-11-02 (включительно) с группировкой по датам.

Столбцы в результате

date

amount - рассчитанное количество чеков

Важно: Обратите внимание, что название столбцов в вашем ответе должно в точности совпадать с условием.

Сортировка
Результат должен быть отсортирован по убыванию количества.

Решение
SELECT
    DATE(date) AS date,
    COUNT(DISTINCT doc_id) AS amount
FROM transactions
WHERE date >= '2019-10-25' 
  AND date <= '2019-11-02'
GROUP BY DATE(date)
ORDER BY amount DESC;




Результат должен быть отсортирован по убыванию количества.
